{% extends '::layout.html.twig' %}

{% block body -%}

<h1>Clients BEH <small>Les Biens d'Equipements Hôteliers</small></h1>
 <ol class="breadcrumb">
              <li><a href="{{ path('commercial_client') }}"><i class="icon-dashboard"></i> Clients</a></li>
              <li class="active"><a href="{{ path('admin_client_new') }}"><i class="icon-file-alt"></i> Ajouter un client BEH</li></a>
            </ol>
          </div>
        </div><!-- /.row -->
        
        
<div class="row">
    {% for entity in entities %}
          <div class="col-lg-3">
            <div class="panel panel-info">
              <div class="panel-heading">
                <div class="row">
                 
                   <img src="{{asset('bundles/managercommercial/images/client/'~entity.id~'.'~entity.url)}}" class="col-lg-12" />
                 
                </div>
              </div>
              <a href="#">
                <div class="panel-footer announcement-bottom">
                  <div class="row">
                    <div class="col-xs-6">
                      {{ entity.name }}
                    </div>
                    <div class="col-xs-6 text-right">
                      
                        <div class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-arrow-circle-right"></i> </a>
                          <ul class="dropdown-menu">
                            <li><a href="{{ path('admin_client_show', { 'id': entity.id }) }}">Voir</a></li>
                            <li><a href="{{ path('admin_client_edit', { 'id': entity.id }) }}">Editer</a></li>

    
                          </ul>
                        </div>
                    </div>
                  </div>
                </div>
              </a>
            </div>
          </div>
         {% endfor %}
        </div><!-- /.row -->
        
    {% endblock %}
