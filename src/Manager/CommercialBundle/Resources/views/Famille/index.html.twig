{% extends '::layout.html.twig' %}

{% block body -%}

 <h1>Familles BEH <small>Les Biens d'Equipements Hôteliers</small></h1>
 <ol class="breadcrumb">
              
              <li class="active"><a href="{{ path('admin_famille_new') }}"><i class="icon-file-alt"></i> Ajouter une famille BEH</li></a>
            </ol>
          </div>
        </div><!-- /.row -->
        
        
<div class="row">
    {% for entity in entities %}
  
        <div class="col-lg-3">
            <div class="panel panel-info">
              <div class="panel-heading">
                <div class="row">
                 <a href="{{ path('famille_categorie', { 'id': entity.id }) }}">
                   <img src="{{asset('bundles/managercommercial/images/famille/'~entity.id~'.'~entity.url)}}" class="col-lg-12" />
                 </a>
                </div>
              </div>
           
                <div class="panel-footer announcement-bottom">
                  <div class="row">
                    <div class="col-xs-6">
                        <a href="{{ path('famille_categorie', { 'id': entity.id }) }}">
                      {{ entity.name }}
                            </a>
                    </div>
                    <div class="col-xs-6 text-right">
                      
                        <div class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-arrow-circle-right"></i> </a>
                          <ul class="dropdown-menu">
                            <li><a href="{{ path('admin_famille_show', { 'id': entity.id }) }}">Voir</a></li>
                            <li><a href="{{ path('admin_famille_edit', { 'id': entity.id }) }}">Editer</a></li>
                             <li><a href="{{ path('famille_categorie', { 'id': entity.id }) }}">categorie</a></li>
    
                          </ul>
                        </div>
                    </div>
                  </div>
                </div>
              
            </div>
          </div>
  
         {% endfor %}
        </div><!-- /.row -->

   
    {% endblock %}
