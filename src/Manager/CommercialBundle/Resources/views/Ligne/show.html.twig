{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Ligne</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ entity.qte }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_ligne') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_ligne_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
