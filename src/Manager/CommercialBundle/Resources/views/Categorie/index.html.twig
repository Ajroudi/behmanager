{% extends '::layout.html.twig' %}

{% block body -%}
 <h1>{{famille.name}} <small>Les Biens d'Equipements Hôteliers</small></h1>
 <ol class="breadcrumb">
              <li><a href="{{ path('commercial_famille') }}"><i class="icon-dashboard"></i> Familles BEH</a></li>
              <li><i class="icon-dashboard"></i> {{famille.name}}</li>
              <li class="active"><a href="{{ path('admin_categorie_new') }}"><i class="icon-file-alt"></i> Ajouter une Categorie BEH</li></a>
            </ol>
          </div>
        </div><!-- /.row -->
 
<div class="row">
    {% for entity in entities %}
          <div class="col-lg-3">
            <div class="panel panel-info">
              <div class="panel-heading">
                <div class="row">
                 <a href="{{ path('categorie_range', { 'id': entity.id }) }}">
                   <img src="{{asset('bundles/managercommercial/images/categorie/'~entity.id~'.'~entity.url)}}" class="col-lg-12" />
                 </a>
                </div>
              </div>
              <a href="#">
                <div class="panel-footer announcement-bottom">
                  <div class="row">
                    <div class="col-xs-6">
                      {{ entity.name }}
                    </div>
                    <div class="col-xs-6 text-right">
                      
                        <div class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-arrow-circle-right"></i> </a>
                          <ul class="dropdown-menu">
                            <li><a href="{{ path('admin_categorie_show', { 'id': entity.id }) }}">Voir</a></li>
                            <li><a href="{{ path('admin_categorie_edit', { 'id': entity.id }) }}">Editer</a></li>
                            <li><a href="{{ path('categorie_range', { 'id': entity.id }) }}">Ranges</a></li>
    
                          </ul>
                        </div>
                    </div>
                  </div>
                </div>
              </a>
            </div>
          </div>
         {% endfor %}
        </div><!-- /.row -->

   
    {% endblock %}      
