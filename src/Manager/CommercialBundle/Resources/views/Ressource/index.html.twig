{% extends '::layout.html.twig' %}

{% block body -%}

 <h1>Ressources BEH <small>Les Biens d'Equipements Hôteliers</small></h1>
 <ol class="breadcrumb">
              <li><a href="{{ path('commercial_ressource') }}"><i class="icon-dashboard"></i> Ressources</a></li>
              <li class="active"><i class="icon-file-alt"></i> Gestion des ressources BEH</li>
            </ol>
          </div>
        </div><!-- /.row -->
        
<div class="row">
{% if is_granted("ROLE_DIRCOMMERCIAL") %}
    {% for entity in entities %}
          <div class="col-lg-3">
            <div class="panel panel-info">
              <div class="panel-heading">
                <div class="row">
                  <div class="col-xs-4">
                    <i class="fa fa-file-text fa-5x"></i>
                  </div>
                 <div class="col-xs-8">
                     <h6> <em>{{entity.name}}</em> </h6>
                         
                  </div>

                </div>
              </div>
              <a href="#">
                <div class="panel-footer announcement-bottom">
                  <div class="row">
                    <div class="col-xs-6">
                        Actions
                    </div>
                        
                          <div class="col-xs-6 text-right">
                      
                        <div class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-arrow-circle-right"></i> </a>
                          <ul class="dropdown-menu">
                            <li><a href="{{ path('admin_ressource_show', { 'id': entity.id }) }}">Voir</a></li>
                            {% if is_granted('ROLE_DIRCOMMERCIAL') %}
                            <li><a href="{{ path('admin_ressource_edit', { 'id': entity.id }) }}">Editer</a></li>
                            {% endif %}
    
                          </ul>
                        </div>
                    </div>
                          
                  </div>
                </div>
              </a>
            </div>
          </div>
         {% endfor %}
        {% endif %}
              
        </div><!-- /.row -->


    {% endblock %}      
       