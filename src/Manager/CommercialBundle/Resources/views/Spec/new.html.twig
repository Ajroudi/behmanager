{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Spec creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_spec') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
